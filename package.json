{
	"name": "obsidian-paste-youtube-timestamp",
	"version": "0.1.0",
	"description": "Converts pasted YouTube timestamp URLs into readable Markdown links",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"devDependencies": {
		"builtin-modules": "3.3.0",
		"esbuild": "^0.28.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "^4.7.4"
	}
}
